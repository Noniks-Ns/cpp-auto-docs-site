const consoleForm = document.getElementById('consoleForm');

const syntaxJSON = './json/CSyntax.json';

function loadSolution(solutions) {

}

function interprete(tokens) {

}

function tokenize(code) {
    
}

consoleForm.addEventListener('submit', (c) => {
    tokenize(c);
});
